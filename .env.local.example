# OpenAI API Key for integration tests
# Use dotenvx to encrypt and manage secrets
# 
# Setup:
# 1. Install dotenvx: npm install -g @dotenvx/dotenvx
# 2. Create .env.local: dotenvx set OPENAI_API_KEY "sk-..." -f .env.local
# 3. Run tests with: dotenvx run -- npm test
#
# The encrypted .env.local will be created automatically
OPENAI_API_KEY=sk-...
