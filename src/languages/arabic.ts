import { PromptType } from '../types/language';

export const getArabicPrompt = (type: PromptType): string => {
    const prompts: Record<PromptType, string> = {
        system: `
أنت مهندس برمجيات ذو خبرة تساعد في إنشاء رسائل الـ commit والـ PR.
مخرجاتك لها الخصائص التالية:

- كتابة عربية واضحة من اليمين إلى اليسار
- تعبيرات دقيقة تقنياً
- تلخيص مناسب للتغييرات
`,
        commit: `
بناءً على الـ diff المقدم، قم بإنشاء رسالة commit بنمط {{style}}.

وصف النمط:
- normal: كتابة تقنية قياسية
- emoji: نبرة ودية مع الرموز التعبيرية
- kawaii: نبرة لطيفة وودية

Diff:
{{diff}}
`,
        prTitle: `
بناءً على الـ diff التالي، قم بإنشاء عنوان طلب السحب (Pull Request).

المتطلبات:
1. يجب أن يكون العنوان موجزاً ويمثل التغييرات بدقة
2. يتضمن بادئة (مثل: "ميزة:"، "إصلاح:"، "تحسين:" إلخ)
3. الكتابة باللغة العربية

Diff:
{{diff}}
`,
        prBody: `
بناءً على الـ diff التالي، قم بإنشاء وصف مفصل لطلب السحب (Pull Request).

المتطلبات:
1. يجب أن يتضمن الوصف:
   - نظرة عامة على التغييرات
   - الغرض من التغييرات
   - نطاق التأثير
   - تعليمات الاختبار (إذا لزم الأمر)
2. الكتابة باللغة العربية
3. استخدام النقاط لتحسين القراءة

Diff:
{{diff}}
`
    };

    return prompts[type] || '';
};